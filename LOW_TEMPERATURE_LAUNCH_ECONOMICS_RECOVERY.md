# Low-temperature launch economics recovery

Run `33320248649` completed the exact predeclared 2026-04-03 through 2026-07-23 HRRR acquisition and produced all 2,240 station/date rows. The economics evaluator then stopped before scoring outcomes because its inherited parser required historical market `status == "settled"`.

Kalshi's REST lifecycle uses `finalized` as the returned terminal market status. `settled` is a query-filter alias. The captured `KXLOWTATL-26JUN29` records have `status == "finalized"`, a nonempty ticker and settlement timestamp, and an exact binary result. `KXLOWTATL-26APR14` uses the separately documented terminal `scalar` result. This recovery therefore changes only the historical terminal-identity parser from the impossible response value `settled` to the documented exact response value `finalized`, while additionally requiring a nonempty settlement timestamp and a documented `yes`, `no`, or `scalar` result. `settled`, `determined`, missing result, missing settlement timestamp, and missing ticker remain rejected. The strategy-level scorer continues to reject a selected scalar lower contract, so scalar admission at the inventory layer cannot create economic evidence.

The completed capture is sealed at `data/results/hrrr-v4-low-temperature-launch-capture-v1`. The successor must verify the archive and member checksums and must not reacquire HRRR or NCEI data. It retains the original predeclaration, window, frozen model, provider inventory, request ceiling, economics gates, and no-retry rule. No strategy outcome was evaluated before the parser exception. This recovery is operational only and does not authorize trading or change production capability.

Run `33324290727` then reached the exact lower contract for `KXLOWTATL-26APR14`, whose finalized result is `scalar`. A scalar settlement is a valid provider inventory identity but is not a binary win/loss and cannot enter the settlement bridge. The launch consumer now records the exact station/date/ticker/status/result/settlement identity as `terminal_scalar_is_not_binary_outcome_evidence`, excludes it from bridge and economics counts, and leaves every predeclared minimum-date, station, mismatch, price, fee, profitability, and drawdown gate unchanged. A selected scalar contract still fails closed in the reusable binary scorer. The run's 201 exact request records are sealed in `data/results/hrrr-v4-low-temperature-launch-partial-cache-v1`; the next successor reuses them locally and makes no artifact-download or repeat request for those URLs.

Run `33324572976` then reached `KXLOWTATL-26JUN30`. The exact event and NWS settlement source exist, but the complete historical market inventory contains zero markets for that event, so no lower-outer contract exists to score, price, or trade. The consumer now records `missing_lower_outer_contract_is_not_executable_evidence` and excludes the station/date; duplicate lower-outer contracts still raise an ambiguity failure. The predeclared bridge-date and all downstream gates remain unchanged. The rolling cache seal now contains 907 exact request records (201 inherited and 706 newly fetched), replacing rather than accumulating the prior cache artifact.
